REQUIREMENTS

The code has been tested to run using:

numpy==1.18.1
matplotlib==3.1.3
pandas==1.0.1

Python version == 3.7.x

As a general note, we believe the code can run on any recent version of the preceding packages.